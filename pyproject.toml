[project]
name = "memory-sweeper"
version = "0.1.0"
description = "Automated leak detection and refactor suggestions for Python code."
authors = [{name = "Axentx"}]
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
