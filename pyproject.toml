[project]
name = "memory-sweeper"
version = "0.1.0"
description = "Utility to map memory leaks to cloud cost impact."
authors = [{name = "AxentX"}]
requires-python = ">=3.9"
license = {text = "MIT"}

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
